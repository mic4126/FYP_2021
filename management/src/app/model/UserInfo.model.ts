export type JWTUserInfo = {
    username: string;
    userType: string;
    projectIds: Number[];
}

export type UserInfo = {
    username:string;
    firstName:string;
    lastName:string;
    email:string;  
    userType:string;  
}